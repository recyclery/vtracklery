<h1>Listing work_times</h1>

<%= paginate @work_times %>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Start at</th>
      <th>End at</th>
      <th>Hours</th>
      <th>Worker</th>
      <th>Status</th>
      <th>Work status</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @work_times.each do |work_time| %>
      <tr>
        <td><%= work_time.start_date_to_s %></td>
        <td><%= work_time.start_time_to_s %></td>
        <td><%= work_time.end_time %></td>
        <td><%= work_time.difference_to_s %></td>
        <td><%=link_to_worker work_time.worker %></td>
        <td><%= work_time.status_name %></td>
        <td><%= work_time.work_status_name %></td>
        <td><%= link_to 'Show', work_time %></td>
        <td><%= link_to 'Edit', edit_work_time_path(work_time) %></td>
        <td><%= link_to 'Destroy', work_time, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= paginate @work_times %>
<%= link_to 'New Work time', new_work_time_path %>
