<%= form_for([@worker, @transaction]) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this worker from being saved:</h2>
      <ul>
      <% @transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :points, "Number of Points" %><br>
    <%= f.text_field :points, type: "number" %>
  </div>
  <div class="field">
    <%= f.label :description, "Description of parts" %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
